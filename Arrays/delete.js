// delete operator

let arr = [23, 45, 5, 6, 7];
console.log(arr);

console.log('Length of array beforer deletion =>  ', arr.length);

delete arr[0]; // length of array is not changed.
console.log('Length of array after deletion =>  ', arr.length);
